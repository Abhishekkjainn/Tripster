import 'package:flutter/material.dart';

class OnwardLuggage extends StatelessWidget {
  const OnwardLuggage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
