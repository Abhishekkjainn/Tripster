import 'package:flutter/material.dart';

class OnwardMeal extends StatelessWidget {
  const OnwardMeal({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
    );
  }
}
